import { Text, View } from "react-native";

export function Groups() {
  return (
    <View className="flex items-center justify-center h-full">
      <Text className="text-red-600 font-bold">Groups</Text>
    </View>
  );
}
